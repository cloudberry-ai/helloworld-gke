apiVersion: v1
kind: Service
metadata:
  name: hello
spec:
  type: NodePort
  selector:
    app: hello
    department: sales
  ports:
  - name: hello-port
    protocol: TCP
    port: 80
    targetPort: 8080